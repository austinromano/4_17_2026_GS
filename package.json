{
  "name": "ghost-session",
  "private": true,
  "scripts": {
    "build": "cd apps/server && echo 'ready'",
    "start": "cd apps/server && npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=20"
  }
}
